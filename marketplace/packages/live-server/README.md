# Live Server

A lightweight SamCode plugin for previewing HTML files and refreshing them automatically while you edit.

This package is intended to behave like a VS Code Live Server style tool, but it should run outside the renderer and stay isolated from the editor UI.
